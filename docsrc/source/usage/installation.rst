Installation
============

Clone repository from `<https://github.ibm.com/vgurev/functionfuse>`_.
Change the directory to the root folder of the package and run

.. code-block:: bash
    
    % pip install -e .


